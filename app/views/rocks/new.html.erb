<h1> Add a Rock </h1>

<%= simple_form_for(@rock) do |f| %>
  <%= f.input :rock_type %>
  <%= f.input :daily_price %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Add Rock" %>
<% end %>

<%= link_to "Back to Home", root_path(@rock) %>
